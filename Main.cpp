#include "IncludeFiles.h"
#include "GasStationComputer.h"
// This is the entry point for the application

int main(void) {
    GasStationComputer gasStationComputer;
    // TODO if there is less than 200 liters of gas in the tank, then have it flash red and signal that we need to fill it up!

    return 0;
}